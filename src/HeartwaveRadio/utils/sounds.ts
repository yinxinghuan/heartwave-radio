import type { Dimension } from '../types';
let ctx:AudioContext|null=null,musicBus:GainNode|null=null;
const get=()=>ctx||(ctx=new (window.AudioContext||(window as any).webkitAudioContext)());
const bus=()=>{const c=get();if(!musicBus){musicBus=c.createGain();musicBus.gain.value=.42;musicBus.connect(c.destination)}return musicBus};
function tone(freq:number,duration:number,type:OscillatorType='sine',gain=.045,delay=0,end?:number,destination?:AudioNode){try{const c=get(),n=c.currentTime+delay,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(freq,n);if(end)o.frequency.exponentialRampToValueAtTime(end,n+duration);g.gain.setValueAtTime(gain,n);g.gain.exponentialRampToValueAtTime(.001,n+duration);o.connect(g).connect(destination||c.destination);o.start(n);o.stop(n+duration)}catch{}}
function noise(duration:number,gain=.018,delay=0){try{const c=get(),length=Math.ceil(c.sampleRate*duration),buffer=c.createBuffer(1,length,c.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<length;i++)data[i]=(Math.random()*2-1)*(1-i/length);const src=c.createBufferSource(),g=c.createGain(),n=c.currentTime+delay;src.buffer=buffer;g.gain.setValueAtTime(gain,n);g.gain.exponentialRampToValueAtTime(.001,n+duration);src.connect(g).connect(bus());src.start(n)}catch{}}
export const resumeAudio=()=>{const c=get();if(c.state==='suspended')void c.resume()};
export const click=()=>tone(600,.045,'sine',.035,0,420);
export const startSound=()=>{noise(.42,.028);tone(220,.28,'sine',.035,.12,440,bus());tone(494,.22,'triangle',.032,.32,660,bus())};
export const pickSound=(combo:number)=>{tone(480,.09,'triangle',.035,0,undefined,bus());tone(760,.09,'triangle',.03,.04,undefined,bus());if(combo>=2)tone(660,.15,'sine',.04,0,990,bus())};
export const addChannelLayer=(kind:Dimension,step:number)=>{const d=.04,l=.018+Math.min(step,4)*.002;if(kind==='warm'){[261.6,329.6,392].forEach((f,i)=>tone(f,.75,'sine',l,d+i*.05,undefined,bus()));tone(130.8,.9,'triangle',.012,d,undefined,bus())}if(kind==='wild'){[220,330,494,370].forEach((f,i)=>tone(f,.16,'square',l*.72,d+i*.11,undefined,bus()));tone(110,.5,'sawtooth',.009,d,165,bus())}if(kind==='quiet'){tone(523.3,1.3,'sine',l*.72,d,undefined,bus());tone(784,1.1,'sine',l*.45,d+.18,undefined,bus());noise(.8,.006,d)}if(kind==='bright'){[392,493.9,587.3,784].forEach((f,i)=>tone(f,.2,'triangle',l,d+i*.085,undefined,bus()));tone(196,.65,'sine',.01,d,undefined,bus())}};
export const playChannelTheme=(kind:Dimension)=>{noise(.55,.012);const p:Record<Dimension,number[]>={warm:[261.6,329.6,392,329.6,440],wild:[220,330,494,370,554],quiet:[392,523.3,659.3,523.3,784],bright:[392,493.9,587.3,659.3,784]},types:Record<Dimension,OscillatorType>={warm:'sine',wild:'square',quiet:'sine',bright:'triangle'};p[kind].forEach((f,i)=>tone(f,kind==='quiet'?.72:.28,types[kind],kind==='wild'?.014:.022,.12+i*(kind==='quiet'?.38:.18),undefined,bus()))};
export const finishSound=()=>[392,494,587,784].forEach((f,i)=>tone(f,.18,'sine',.04,i*.09,undefined,bus()));
export const timeoutSound=()=>tone(180,.32,'sawtooth',.025,0,90,bus());
export const publishSound=()=>{tone(740,.11,'sine',.04,0,undefined,bus());tone(1110,.22,'sine',.04,.08,undefined,bus())};
